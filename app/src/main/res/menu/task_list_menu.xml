<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- 할 일 타입 필터링 서브메뉴 -->
    <item
        android:id="@+id/action_filter_type"
        android:title="필터"
        android:icon="@drawable/ic_filter_list"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/action_show_all_types"
                android:title="모든 할 일"
                android:icon="@drawable/ic_list" />
            <item
                android:id="@+id/action_show_collaboration_only"
                android:title="협업 할 일만"
                android:icon="@drawable/ic_collaboration" />
            <item
                android:id="@+id/action_show_local_only"
                android:title="내 할 일만"
                android:icon="@drawable/ic_person" />
        </menu>
    </item>

    <!-- 동기화 관련 메뉴 -->
    <item
        android:id="@+id/action_sync"
        android:title="동기화"
        android:icon="@drawable/ic_sync"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/action_manual_sync"
                android:title="수동 동기화"
                android:icon="@drawable/ic_sync" />
            <item
                android:id="@+id/action_sync_info"
                android:title="동기화 정보"
                android:icon="@drawable/ic_info" />
        </menu>
    </item>

    <!-- 더보기 옵션 -->
    <item
        android:id="@+id/action_more"
        android:title="더보기"
        android:icon="@drawable/ic_more_vert"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/action_delete_all"
                android:title="모든 할 일 삭제"
                android:icon="@drawable/ic_delete" />
            <item
                android:id="@+id/action_export"
                android:title="내보내기"
                android:icon="@drawable/ic_share" />
            <item
                android:id="@+id/action_settings"
                android:title="설정"
                android:icon="@drawable/ic_settings" />
        </menu>
    </item>

</menu>